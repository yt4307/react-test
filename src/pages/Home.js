import React from "react";
import styled from "styled-components";

const Main = styled.div`
    text-align: center;
`;

const Home = () => {
    return (
        <Main>
            홈 입니다.
        </Main>
    );
};

export default Home;